---
layout: base.njk
---

{% set currentLocale = locale or page | getLocale %}
{% set alternateLocale = 'de' if currentLocale == 'en' else 'en' %}

{% include "navigation.njk" %}

<main id="main-content" role="main" class="container mx-auto max-w-6xl px-6 py-12 flex-grow">
  {% block content %}
    {{ content | safe }}
  {% endblock %}
</main>

{# Simple Footer #}
<footer role="contentinfo" class="p-8 mt-16 border-t border-white/10 text-center text-sm opacity-60">
  <div class="container mx-auto max-w-6xl">
    <p>&copy; {{ year | default(2026) }} Veit Helmer. {{ 'footer.copyright' | t(currentLocale) }}</p>
    {% block footerContent %}{% endblock %}
  </div>
</footer>
